<div class="banner-home" >
  <div class="video">
    <video autoplay loop>
    <source src: url(<%= video_tag("http://res.cloudinary.com/rebekah/video/upload/v1497018356/Sketch_c1crga.mp4")%>)>
    </video>
  </div>
  <div class="banner-content">
    <h1>the online art school</h1>
      <br>
      <div class="banner-cta">
        <%= link_to "LEARN", courses_path, class: "btn btn-frida" %>
        <% if user_signed_in? %>
          <% if current_user.teacher %>
            <%= link_to "TEACH", new_course_path, class: "btn btn-frida" %>
          <% end %>
        <% end %>
      </div>
  </div>
</div>


<%= content_for(:navbar, "navbar-home") %>
<%= content_for(:logo, "logo copy white.png") %>
