<% content_for(:after_js) do %>
  <script>
    $( document ).ready(function() {
      // window.sr = ScrollReveal({ reset: true });
      // sr.reveal('.courses-box', { duration: 200 });
      // sr.reveal('.courses-box', { duration: 2000 }, 100);
      new WOW().init();
    });
  </script>
<% end %>
